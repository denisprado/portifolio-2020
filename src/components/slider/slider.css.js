import styled from 'styled-components';

export const SlideContainer = styled.div`
  position: relative;
  height: 100vh;
  width: 100vw;
  margin: 0 auto 80px;
  overflow: hidden;
  white-space: nowrap;
`;
